// Slect the elemt or group of elements the way we want
// Decide the effect we want to apply to the slection

// childNOdes - returns all childNodes including whitespace which is treated as a text node

// children
// firstChild
// lastChild

const result = document.querySelector('#result');
const allChildren = result.childNodes;

const children = result.children;

console.log(children[3]);
